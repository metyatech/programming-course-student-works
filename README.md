# programming-course-student-works

Student works (static) for the Programming course.

## Publish

- GitHub Pages is deployed from `dist/` via Actions.
- `works-index.json` is generated automatically on deploy and consumed by the course site to render the works list.

